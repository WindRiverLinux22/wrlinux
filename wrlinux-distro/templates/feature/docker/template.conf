# Copyright (C) 2019 Wind River Systems, Inc.

FEATURE_DOCKER_MSG = "This architecture is not supported for the feature/docker template."
FEATURE_DOCKER_MSG:x86-64 = ""
FEATURE_DOCKER_MSG:armv7a = ""
FEATURE_DOCKER_MSG:armv7ve = ""
FEATURE_DOCKER_MSG:armv7athf-neon = ""
FEATURE_DOCKER_MSG:armv8a = ""
FEATURE_DOCKER_MSG:aarch64 = ""
FATAL_CONFIG_BANNER[feature_docker] = "${FEATURE_DOCKER_MSG}"

KERNEL_FEATURES += "features/lxc/lxc-enable.scc"
KERNEL_FEATURES += "features/netfilter/netfilter.scc"

# Always search the meta-virtualization linux-yocto layer because that
# is the origin of the docker.cfg
EXTRA_KERNEL_FILES =. "${@'${BBFILE_PATTERN_virtualization-layer}'[1:]}recipes-kernel/linux/linux-yocto:"
EXTRA_KERNEL_SRC_URI += "file://docker.cfg"

# wrlinux additional docker configuration path
EXTRA_KERNEL_FILES =. "${LAYER_PATH_wrlinux-distro}/templates/feature/docker/files:"
EXTRA_KERNEL_SRC_URI += "file://docker-aux.cfg"

PNWHITELIST:virtualization-layer:append = "\
  docker \
  cgroup-lite \
  runc-docker \
  containerd-docker \
  containerd-opencontainers \
  lxc \
  go-distribution \
  go-logrus \
  go-systemd \
  go-context \
  go-connections go-capability go-pty go-fsnotify go-patricia go-dbus  \
  go-mux \
  go-cli \
  compose-file \
  grpc-go \
  notary \
  tini \
  "
PNWHITELIST:networking-layer:append = " bridge-utils"

# Use the same version of docker as the kubernetes template
PREFERRED_VERSION_docker = "20.10.12"

# Whitelist for python3-docker-compose
PNWHITELIST:meta-python += 'python3-bcrypt'
PNWHITELIST:meta-python += 'python3-colorama'
PNWHITELIST:meta-python += 'python3-jsonpointer'
PNWHITELIST:meta-python += 'python3-jsonschema'
PNWHITELIST:meta-python += 'python3-pyrsistent'
PNWHITELIST:meta-python += 'python3-rfc3987'
PNWHITELIST:meta-python += 'python3-setuptools-scm'
PNWHITELIST:meta-python += 'python3-strict-rfc3339'
PNWHITELIST:meta-python += 'python3-vcversioner'
PNWHITELIST:meta-python += 'python3-webcolors'
PNWHITELIST:meta-python += 'python3-websocket-client'
PNWHITELIST:meta-python += 'python3-distro'
PNWHITELIST:meta-python += 'python3-wcwidth'
PNWHITELIST:meta-python += 'python3-docopt'
PNWHITELIST:meta-python += 'python3-texttable'
PNWHITELIST:meta-python += 'python3-paramiko'
PNWHITELIST:meta-python += 'python3-pynacl'
PNWHITELIST:meta-python += 'python3-wheel'
PNWHITELIST:virtualization-layer += 'python3-cached-property'
PNWHITELIST:virtualization-layer += 'python3-docker'
PNWHITELIST:virtualization-layer += 'python3-docker-compose'
PNWHITELIST:virtualization-layer += 'python3-docker-pycreds'
PNWHITELIST:virtualization-layer += 'python3-dockerpty'
PNWHITELIST:virtualization-layer += 'python3-nacl'
PNWHITELIST:virtualization-layer += 'python3-fastentrypoints'
PNWHITELIST:virtualization-layer += 'python3-dotenv'
PNWHITELIST:openembedded-layer += 'libsodium'
